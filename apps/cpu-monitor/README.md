# CPU Monitor

Lightweight CPU load meter for development on real G3 hardware — baseline calibration, smoothed history, and an on-screen bar while profiling the other apps.

## Build

CodeWarrior for Mac OS 9 — create a new project and add `src/cpu-monitor.c` as the only source file.

## Source

`src/cpu-monitor.c`

## Releases

Tag releases as `cpu-monitor/v0.1`, etc.
